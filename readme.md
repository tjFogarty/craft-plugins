# Craft Plugins

<a href="https://codepen.io/tjFogarty/project/full/ZdYLPK" target="_blank" rel="noopener">See a demo</a>

## Setup

- Clone the repo and run `npm install` or `yarn`.
- Run `npm run serve` to start development
- Run `npm run build` to build a dist version
